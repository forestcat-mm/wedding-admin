-- ===== 配席：卓の初期データ（通常は不要） =====
-- レイアウトはアプリが「卓数」から自動生成する（最大 3行×4列・高砂は最前面中央）。
-- 配席タブを開いたとき seating_tables が空なら、高砂＋min 卓（= ceil(出席人数 / 10)）を
-- 自動で作るので、このファイルを実行する必要はない。
-- 作り直したいときは下の delete を外して実行し、あとは画面の「卓数」で調整する。

-- delete from seating_assignments;
-- delete from seating_tables;

-- 高砂だけ入れておく例（丸卓はアプリが作る）
insert into seating_tables (label, capacity, shape, x, y, w, h, sort_order)
values ('高砂', 2, 'head', 50, 10, 21.4, 5.6, 0);

-- 参考：卓数ごとの行構成（アプリ内 ROW_PLAN と同じ）
--   1〜4卓：1行(N) ／ 5:3,2 ／ 6:3,3 ／ 7:4,3 ／ 8:4,4
--   9:3,3,3 ／ 10:4,3,3 ／ 11:4,4,3 ／ 12:4,4,4
-- 各行の x（％）：4卓 12.5/37.5/62.5/87.5、3卓 25/50/75、2卓 33/67、1卓 50
-- 行の y（％）：行数 R のとき 59 - (R-1)*12.5 から 25 きざみ
